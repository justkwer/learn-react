import React from 'react';
import Navigation from './Navigation/Navigation';

const Header = () => {
  return (
    <header>
      <section>
        <Navigation />
      </section>
    </header>
  );
};

export default Header;
